npm install mongoose --save
npm install @types/mongoose --save-dev
npm install @types/mongodb --save-dev

--> using Q for the promise library in mongoose. To use the q library we need to first install it via npm:
npm install q --save

npm install body-parser --save
npm install cookie-parser --save
npm install errorhandler --save
npm install express --save
npm install method-override --save
npm install morgan --save

--> Testing with Mocha and Chai
npm install mocha --save-dev
npm install chai --save-dev
npm install @types/mocha --save-dev
npm install @types/chai --save-dev

--> tsconfig.json and package.json scripts

--> npm install --save-dev tslint, tslint.json

---> using “a more TypeScript approach” to creating my tests using the project mocha-typescript.
npm install mocha-typescript --save-dev

---> use concurrently to run multiple scripts in one command in package.json
npm install -g concurrently
npm install concurrently --save


